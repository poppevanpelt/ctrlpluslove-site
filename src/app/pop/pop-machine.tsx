"use client";
import { useState } from "react";
import styles from "./page.module.css";
const lines=["OBJECT LEFT THE BUILDING.","THEORY SUCCESSFULLY DAMAGED BY REALITY.","ONE LESS THING WAITING FOR PERMISSION.","THE FACTORY HAS BEEN INFORMED."];
export default function PopMachine(){const [count,setCount]=useState(0);const [thing,setThing]=useState("");const [receipt,setReceipt]=useState<{serial:string;line:string;thing:string}|null>(null);const pull=()=>{const n=count+1;setCount(n);setReceipt({serial:`POP-${String(n).padStart(4,"0")}`,line:lines[n%lines.length],thing:thing.trim()||"UNSPECIFIED OBJECT"});};return <section className={styles.machine}><div className={styles.unit}><div className={styles.guard}>LIFT GUARD BEFORE OPERATION</div><button className={styles.lever} onClick={pull} aria-label="Acknowledge shipment"><span/><b>PULL</b></button><div className={`${styles.projectile} ${receipt?styles.fired:""}`} aria-hidden="true"/><label>WHAT SHIPPED?<input value={thing} onChange={e=>setThing(e.target.value)} placeholder="One real thing"/></label><small>TOTAL CEREMONIES PERFORMED · {String(count).padStart(3,"0")}</small></div><div className={styles.chute}>{receipt?<article className={styles.receipt}><span>SHIPMENT RECEIPT</span><strong>{receipt.serial}</strong><p>{receipt.thing}</p><b>{receipt.line}</b><small>{new Date().toLocaleString()}</small></article>:<p>RECEIPT CHUTE EMPTY</p>}</div></section>}
